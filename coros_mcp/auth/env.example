# Copy to ~/.config/coros-mcp/.env (mode 0600).
# Do NOT commit this file or place it inside a git-synced Obsidian vault.
# Prefer: coros-mcp auth  (stores tokens outside the vault/repo)

COROS_EMAIL=you@example.com
COROS_PASSWORD=yourpassword
COROS_REGION=eu
